import * as React from 'react';
import './Entry.scss';

class Entry extends React.Component {
    render() {
        return (
            <div>
                Entry Page
            </div>
        );
    }
};

export default Entry;